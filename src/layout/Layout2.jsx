import { Outlet } from "react-router-dom"

const Layout2 = () => {
    return (
        <>
        {/* Navbar */}
        {/* Aside */}
        <main>
            <Outlet />
        </main>

        </>
    )
}

export default Layout2;
